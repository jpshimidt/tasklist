const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'migration-path': (__dirname, 'src', 'database', 'migrations'),
  'seeders-path': (__dirname, 'src', 'database', 'seeds')
};
