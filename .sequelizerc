const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'migrations-path': (__dirname, 'src', 'database', 'migrations'),
  'seeders-path': (__dirname, 'src', 'database', 'migrations')
};
